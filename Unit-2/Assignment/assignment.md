
---

## **2 Marks Questions**

* Define **HTML**.
* What is the purpose of the **DOCTYPE** declaration?
* Define **head** and **body** sections of an HTML document.
* Name two **text formatting tags** in HTML.
* Difference between **ordered and unordered lists**.
* Define **hyperlink**.
* Define **semantic HTML**.
* Name two **image file formats** supported in HTML.

---

## **5 Marks Questions**

* Explain the **basic structure of an HTML document** with an example.
* Difference between **absolute and relative hyperlinks** with examples.
* Difference between **inline, block, and semantic elements**.
* Explain **tables in HTML** and their basic tags (`<table>`, `<tr>`, `<td>`).
* Difference between **forms and input types**.
* Explain the use of **images and multimedia tags** (`<img>`, `<audio>`, `<video>`).

---

## **15 Marks Questions**

* Create a **sample HTML page** including: headings, paragraph text, lists, images, links, and a table.
* Explain **semantic HTML** with examples (`<header>`, `<footer>`, `<article>`, `<section>`).
* Create a **form with input fields, checkboxes, radio buttons, dropdowns**, and a submit button using HTML.
* Discuss **HTML links and navigation**, including internal, external, and anchor links with examples.

---
